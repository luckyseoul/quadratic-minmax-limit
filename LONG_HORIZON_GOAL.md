# Long-horizon research objective

Answer the original convergence question in `GOAL.md` and `CORE.md`.
The original MO limit is OPEN. Do not assume a value or an optimizer family.

Keep one bounded mathematical implication active. Prefer an all-orders
argument controlling the normalized optimum to a larger finite search.
Explore a route only for the implication it actually proves; abandon its
mandatory status if that implication was assumed rather than established.

The historical Paley program is preserved in `ARTIFACTS.md`. Residual (ii)
and the minimal-four-gap entry bridge are separate open statements inside
that program. Neither is known necessary for a different proof of the
original limit. The same caution applies to direct doubling/tripling and
their current constructions.

Use the result labels in `AGENTS.md`. Keep exact evidence and record a
changed premise before rerunning work. Global closure belongs only to a
reviewed complete proof, independently of all optional-route predicates.

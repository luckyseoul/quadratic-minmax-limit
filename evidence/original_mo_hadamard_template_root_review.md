# Root complete review: actual Hadamard templates and Boolean attainability

2026-09-05. Mathematical PASS in the scopes printed in the three proofs.
Original all-orders convergence remains OPEN.

## Complete reads and review roles

The root completely read:

- The 351-line sparse-flip construction, SHA256
  `0d2355f94734b4c1e950c1e05c6df75df38b5ce181ba7fce550a4245e11328ed`.
- The initial 317-line Gamma theorem, SHA256
  `0306c8be7496b03d9d785c2d55145170784fa3fb2b3882a7d46b33ff1540348a`,
  and both changes in its final 324-line version, SHA256
  `bd5997203c52895744a078048e206241996c46ef485e8975d7955b73be41f1c6`.
  Reversing exactly the execution-disclosure replacement and explicit
  uniform-margin append reconstructs the initial hash. Thus the remainder
  of the completely reviewed proof is byte-identical.
- The complete 363-line modified Boolean-algebra proof, SHA256
  `68ce3f2f2a8fa2280208a9e145f508b6c2b2520d81e83185f579aaac89838a5d`.

The root proposed the sparse-flip realization direction and participated
in the finite completion discussion. For the modified construction the
root contributed the PSD completion equality and symmetric dilation
argument; the exact worker supplied the invariant Boolean algebra,
integrated construction, and complete proofs. These root checks are
collaborative verification, not additional independent author-level
reviews. The Gamma proof was developed by the proof worker, with prior
cross-rounding and constant discussion involving the exact worker.
Its exact-worker full final-file review explicitly discloses that role.
The docs worker separately performed complete independent reads of all
three final proofs; its own receipts record those conclusions.

## Sparse-flip construction

The tensor Hadamard is symmetric, orthogonal after normalization, and
regular. Its block-constant compression is exactly O_p. The rank-p
implant consequently gives the literal direct sum C plus an orthogonal
complement; both lower and upper singular outliers are retained.

The sign-flip probabilities lie in [0,1] for the stated eventual orders.
Their entrywise expectation, centered bound two, and both row and column
variance sums agree with the printed formulas. The root checked the
ENTIRE primary statement of Tropp's rectangular matrix Bernstein
Theorem 1.6 in the author-hosted source linked in the proof. This is an
imported published theorem, not an independent review of its full paper.
The concentration exponent tends to infinity faster than log n.
Its good event and the Markov flip-count event intersect with probability
at least one half, giving deterministic existence and the stated epsilon_n.

The vector-SDP Lipschitz bound is n times the operator perturbation.
Repeated template vectors attain the model value because the actual
block sums are m C_ij. The result is asymptotic scalar-dual optimality,
not exact scalarity of an optimal finite-n diagonal.

The upper completion follows from the actual block means and orthogonal
perpendicular components. Its direction is upper, not lower.
For the lower one, the explicit balanced tensor sign witness has zero
mean in every block and is mapped exactly to its negative. This verifies
the background value one without an unattained sphere argument.

## Finite Gamma theorem

For scalar-optimal finite templates tau(C)=p q, both optimal-frame
equations follow from equality in Cauchy--Schwarz.
They give exactly the same matrix M for U and V, including deficient rank.
Top-eigenvector linear rounding is cube-admissible and bounds the
normalized second frame moment by (Gamma-1)/(q-1).

The nonlinear Gaussian cross term retains all residual signs, using
conditional expectation and the operator bound. The squared clipped
norms need not be equal: only their degree-two Hermite components cancel
pointwise. All remaining even-Hermite cross covariances are nonnegative,
including negative and endpoint Gaussian correlations. Their absolute
upper R rho^2 gives the stated variance bound and the factor one half
in the completion loss. The implicit inequality, explicit root formula,
and zero-variance limiting cases agree.

For clipping at one, integration by parts gives all four printed
moments. Alternating-series enclosures have the correct parity and
Machin subtraction directions. The strict threshold gives Gamma>283/200
for q>=5/2. This remains a lower bound on an UPPER-CERTIFICATE functional;
it does not lower-bound the Boolean norm of an arbitrary actual matrix.

## Modified Boolean algebra and actual lower

The permutation-modified base fixes both 1 and w. Tensor monomials span
the entire algebra of functions of its k binary variables. Every such
Boolean function is therefore fixed by the actual operator.

Partitioning the first r=2 ell bits gives p=4^ell equal-size blocks.
The remaining patterns occur with equal multiplicity. This proves
independent exact mean grids, their density, and the nested-plus-set
overlap 1-|a_i-b_i|. The compressed background is I_p, not O_p; the
implant and its probabilities correctly use C-I_p.

The actual energy contains the essential subtraction a^T b/p.
The dense grid maxima and operator perturbation give the genuine
Lambda_I lower. Bernoulli covariance and two Cauchy--Schwarz steps
prove Lambda_I<=Gamma for arbitrary C. For symmetric PSD C,
PSD Cauchy--Schwarz plus arithmetic-geometric mean gives equality
with the same-vector quadratic functional. The actual Boolean limit
then follows from the two correctly directed bounds.

The clipping lower for an indefinite symmetric C uses an explicit
POSITIVE top frame CW=qW. Its residual is bounded below by -q times
its squared norm. Bipartite scalar SDP optimality alone is not used
as a substitute for this extra positive-frame condition.

Symmetric dilation has the stated positive frame. Duplication makes
its dimension a power of four and preserves the finite Boolean/SDP
ratio, since the two bilinear terms optimize independently. This
changes the template and large sign family; the resulting actual
norm exclusion is restricted to that constructed family.

## Exact check and remaining scope

The root completely read the 105-line exact-Fraction certificate before
its one bounded remote execution, and all 28 individual result entries
afterward. Its source SHA256 is
`d3af3d3bac9ba4d73a7589ba9ed4ff6261fde3263c64d04de36da7f36a1c65d3`;
the stored result SHA256 is
`fbc10c4760d963f9364dca586cca3d8df5692ab786cd155634a651fac3a62d9d`.
All checks passed on soulkiller. Input hashes were verified before and
after, the result transfer matched, and the recorded process was absent
after completion. The modified construction reuses the same enclosures;
it did not cause a second run.

No sign matrices were sampled or enumerated. No solver, numerical
integration, floating-point evaluation, or optimizer search was used.
The certificate checks its printed finite rational comparisons, not
the analytic construction or an all-orders theorem.

These results show actual realizability of a flat bulk and finite
outliers with asymptotic scalar SDP optimality, and identify genuine
Boolean restrictions for a specified invariant-algebra model. They do
not construct the problematic low-norm template, classify arbitrary
cross optimizers, identify compatible source/joint shells, evaluate
the general conditional Gaussian upper, or prove original convergence.

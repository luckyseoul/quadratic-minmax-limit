# Independent full review: actual central-cell linear-width boundary

2026-09-06. Result: PASS, no correction identified.

Frozen source fully read, all 149 lines, without truncation:

    /tmp/original_mo_actual_central_cell_linear_width_boundary.md
    SHA256 361f3d83de482ec2fac898c615f6277607873a15c64ffa6d6de101d20f41937e

The complete 381-line weighted-shell prerequisite was also fully read
in this work sequence and its frozen hash was rechecked:

    /tmp/original_mo_diagonal_majorizer_weighted_shell_upper.md
    SHA256 9aec82a5e808837ea626f2fd85f526cda1fffe883929711dfc2c6f396392f15f

This is an independent full check of the NEW central-cell argument.
Root supplied the task outline; this reviewer supplied no derivation,
revision, or author contribution to this new source before its freeze.
Earlier contributing roles in other Gaussian/source proofs are not
claimed as independent reviews of those older prerequisites.

Checks:

- The unweighted second-moment constants are 2n(n-1), 2n(n-1), n^2;
  the weighted constants are at most 2n, 2n, n. Chebyshev at the two
  stated thresholds gives exactly the valid total bound 10/sqrt(n).
- Every defining original and weighted energy is unchanged by the
  simultaneous global sign reversal. Both the central union and all
  final refined cells are consequently sign-symmetric.
- The iid Gaussian maximizing sign vector is uniform and independent
  of every coordinate magnitude. The lost central probability can
  therefore be multiplied by the expected full-cube width. The
  complementary central maximum is nonnegative by sign symmetry.
- Intersecting with the central set preserves the polynomial cell
  count. Representatives chosen inside final intersections give
  actual positive covariances and the stated O(k n^(3/4)) deviation.
- Gaussian selection among iid-field cell maxima uses Lipschitz
  constant n sqrt(2w), giving 2n sqrt(w log m); no independence
  between cell maxima is used.
- Scalar-I square-root coupling is valid for singular PSD M_j and
  gives the displayed 2sqrt(2k)n^(11/8) error. Constant marginal
  variances give the matching exact upper on every actual field.
- The central offsets are O(n^(5/4)). Cell sign symmetry yields
  max|a_j+g_j^Tz|=|a_j|+max g_j^Tz exactly, so they do not alter
  the normalized leading central-field width.
- Comparing 2sqrt(kappa w) with the requested 2sqrt(2)alpha scale
  gives the stated necessary condition w<=2alpha^2/kappa for this
  particular field-upper architecture.

The source's scope distinctions are correct and essential. It gives
an actual LINEAR-FIELD saturation theorem, not a lower bound on the
original bilinear Gaussian cross process, and not an impossibility
result for biased thresholds, other comparisons, or original MO
convergence. No source or conditional optimality premise was smuggled
into the moment or field calculations.

No mathematical program, numerical check, solver, or test ran on any
host. Only this /tmp review receipt was written by this reviewer.
The frozen source and all canonical files remain unchanged.

# Third p23 post-band endpoint: checkpoint and acceptance gate

Classification: **open reduction**, with a passing executable certificate
candidate. Proposition 15.771 is **REVIEW_PENDING**, not a reviewed theorem.
The candidate endpoint is `p=23,t=11,k=114`; the preceding endpoints
`t=9,10` are closed by Propositions 15.769--15.770.

The source, nine focused tests, and evidence JSON are preserved. The current
audit found no counterexample or definite mathematical obstruction, but
the following bridges are not yet explicit enough to claim closure:

1. Derive the general-slice equality reduction for boundary sizes `b=4,20`.
2. Show that every required middle-boundary configuration lies in a covering
   swap cube to which the exact even-half rank certificate applies.
3. Supply the phase-zero scaled-mass-32 bridge: only `b=0,2,22` survive
   the floors; the latter two leave lift mass `8<20`; `b=0` invokes the
   Proposition 15.752 `p+9` exclusion.

Acceptance condition: complete and independently review all three bridges,
replay the source/test/evidence package, and synchronize the canonical
frontier before changing the public endpoint flags. An independent hardware
replay alone is not a proof of an omitted implication. Internal `proved`
fields describe the existing component certificate checks; top-level
`proved`, `p23_k114_closed`, and `all_boundary_sizes_excluded` remain false.

Changed premise for mesh work: this is verification of the previously
implicit all-boundary bridges, not a repeat of the finished p23 five-set
moment census. The full mesh should receive complementary exact-arithmetic
checks and adversarial proof tasks. No graph, orbit, or later-layer search is
authorized by this acceptance gate.

Residual (ii), E1, `L=1/2`, and the original convergence question remain open.

# Root integration review: threshold-optimized Gaussian reduction

2026-09-05. Mathematical PASS at the precise scope below.

The root author checked the complete 317-line shifted-sign proof and
independently read the complete 307-line initial threshold-covariance
proof. The latter's final changed sections through equation (21) were
read separately and then its complete final 338-line proof was reread.
The complete smooth-activation and Gaussian Holder prerequisites were
also checked. The canonical imported files are byte-identical to:

- `NOTE_2026-09-05_SHIFTED_SIGN_GAUSSIAN_UNIVERSALITY.md`, SHA-256
  `a3ed6d9c3ee73b863c91d069e75baf9973911318a8efe9156ca61e30f55d7e25`.
- `NOTE_2026-09-05_SHIFTED_THRESHOLD_COVARIANCE_REDUCTION.md`, SHA-256
  `74457650912a515eaf6a209b184e5c1404a13fc48c68464068871ebd61236680`.

The shifted-sign theorem has two independent full-read PASS receipts.
The covariance theorem has root and exact-worker independent full-read
PASS reviews. Each receipt records the preceding reviewed versions and
the precise final deltas. They do not substitute a checker or sampling
run for a proof read.

## Checks affecting the conclusion

Conjugation by the threshold orientation preserves the latent covariance
operator bound. The deterministic mean changes the fixed prior exactly.
OU smoothing uses the shift `h sqrt(1+epsilon^2)`, retains the exact mean,
and has uniform derivative bounds. Equal-threshold disagreement is
maximized at threshold zero. All integer Hermite Schur powers are PSD,
so their covariance decrease is controlled without an oddness premise.
The residual absolute bound is two; its resulting Bernstein constant
and the explicit growing-temperature rate were checked.

The Gaussian threshold covariance has a nonvanishing even-Hermite term.
The proof retains its explicit PSD part and realizes it as scalar, row,
and column Gaussian multipliers of A. Polarization gives the required
`beta(A)<=4 Phi(A)` bound; Gaussian maxima then control its actual
Boolean norm. The remaining operator error is at most `18/n`, including
the small endpoint n=2. Gaussian convex order gives the stated expected
maximum error with arbitrary fixed offsets. Both cross diagonals cost
only `2n` in total expectation regardless of their correlations.

The independent-sign exponential-maximum cap applies to every exact
original minimizer. The combined uniform error therefore allows an
infimum over all deterministic thresholds before sampling, with absolute
raw error `D n^(16/11)`. No infimum attainment or disorder-adaptive choice
is needed. All matrices generated by the threshold law are admissible
complete order-2n signings.

## Scope and next implication

The two notes prove genuine quenched and expected-norm reductions,
including the threshold-optimized upper bound for m_(2n). They do not
evaluate its Gaussian right side against `2 sqrt(2) m_n`, sign the
current-posterior mean/noise derivative, or prove original convergence.
The source, tests and global proof-status predicates are unchanged.
The next implication remains an evaluated order comparison with enough
error control to imply the original all-orders limit.

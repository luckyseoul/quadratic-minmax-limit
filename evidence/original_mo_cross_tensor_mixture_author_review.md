# Collaborative author receipt: cross tensor mixture and sign defect

2026-09-05. This is an authorship, scope, and self-review receipt, not an
independent review.

Source: `/tmp/original_mo_cross_tensor_mixture_sign_defect.md`

Source length: 351 lines.

Source SHA256:
`66ed4d02f4cdd7d323ae5f0c717993bc3453d59dcb3a1e00b2e2c64720d424da`

The author/integrator was `optimized_profile_exact`. The
`optimized_profile_docs_gate` collaborator supplied the clean
odd-tensor/direct-sum construction with
`a=arsinh(1-t)` and cross Gram `sin(a V)+t W`, and identified the
already reviewed real-entry interpolation lemma as a sharper route
to the Boolean-cap Taylor remainder. The integrator supplied the
cap-free weighted residual proof of `N_- <= (tau-S)/2`, its scalar
optimal-diagonal improvement, and the combination with Taylor's
theorem, source normalization, and the necessary normalized curve.
The parent and both collaborators independently checked intermediate
constants and endpoint deductions while the argument was developed.

The integrator wrote and then fully reread every line of the final
351-line source. The parent subsequently reported a full-read PASS
with no correction. The `optimized_profile_proof` collaborator has
also reported an independent full-read PASS; its separate formal
review receipt records that independent review.

The source proves finite-order sign-defect and tensor-mixture
inequalities for actual complete cross sign matrices. Under only
`beta(B) <= C n^(3/2)`, it gives an `O_(C,t)(n^(5/4))` absolute
Taylor remainder and a necessary limiting Boolean/SDP versus
canonical-gap curve. The scalar-optimal-diagonal statement is
explicitly conditional. The source does not evaluate the sharp
Gaussian shell width, establish realizability of abstract spectral
profiles, or prove the original MO limit.

No numerical experiment, solver, mathematical script, test run, or
remote mathematical job was used in this analytic derivation. Local
tools only read or wrote proof text and measured its byte hash and
line count. The source is frozen at the hash above; later scalar
moment refinements are not silently incorporated into this receipt.

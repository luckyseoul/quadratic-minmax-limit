# p=19 all-b2 Boolean and infinity-degree reduction

**Date:** 2026-08-29
**Proposition:** 15.697
**Status:** proved structural reduction; profile and endpoint remain open

After Proposition 15.696, the unique slack-20 survivor has

```text
phase zero: {0:5,16:5}
phase one:  {2:10}.
```

Nine phase-one `b=2` directions attain their pointwise floor
`A0=(t-1)^2`. The tenth has scaled mean 38, so

```text
A=A0+2B,   B>=0 integral quadratic,   E[B]=5/19.
```

The sharp paired-cube and stabilizer bounds imply `max(B) in {1,5}`. If
`max(B)=5`, apply the exact p=19 stabilizer identity at a maximum point
`X0`. Its nodes and weights are

```text
t=4: 0,   t=5: 18/19,   t=10: 1/19.
```

The endpoint contribution is already `5/19`, so nonnegativity forces `B=0`
at every point of the `|X cap X0|=5` layer. A fixed 152-row pair-inclusion
minor from that layer has rank 152 modulo two. The 19-dimensional kernel is
exactly

```text
B=(t-5)L,   degree(L)<=1.
```

Put `R=2L`. Integrality on layers four and six makes all within-part
coefficient differences integral. At a layer-nine neighbour of `X0`, every
cross difference `D_ij` is therefore an integer in `{0,1,2}`. Layer eight
forces every sum of two disjoint entries to be zero or two. The odd-parity
case is the all-one matrix. In even parity, writing `D=2E`, an additive
Boolean matrix is determined by its first row and column. A complete
`2^(10+9-1)=262,144` candidate audit leaves exactly 20 labelled matrices:
the zero matrix, ten single full rows, and nine single full columns. Together
with the all-one matrix these are four symmetry types and 21 labelled
matrices. Explicit replacements give `B=-1` on layer six for the all-one
type and on layer four for every even type. Thus `max(B)=5` is impossible
and `B` is Boolean.

The five phase-zero `b=0` directions are pointwise zero. Their coefficient
identities first reduce the infinity degree to

```text
I=0,10,20,28,38.
```

For such a direction, the signed cross coefficient is `g-h_s-h_t`, where
`sum h_s=I`. Exact enumeration of integer partitions gives:

```text
I    min sum_{s<t}|g-h_s-h_t|    available cross edges
0                  0                       74
10                81                       65   impossible
20                18                       56
28                81                       47   impossible
38                 0                       38
```

Hence only `I={0,20,38}` remain.

## Conditional Boolean catalog

The Filmus--Vinciguerra restriction-threshold result for Boolean functions
on a slice, as linked from Filmus's publication page, says that the
restriction threshold equals the junta threshold `2d`. The linked note was
not retrievable from its SharePoint host during this audit, so this result is
not imported into the unconditional proposition. Conditional on its stated
theorem, applying `d=2` to the complementary slice `J(19,9)` extends `B` to
a Boolean degree-two cube function. The
degree-two cube relevant-variable bound is four. An exhaustive Möbius audit
of all `2^16=65,536` four-variable truth tables finds exactly 30 of slice
density `5/19`, generated by two essential constructions:

```text
two-literal conjunctions;
nonconstant three-variable antipodal pairs.
```

On 19 labelled fibres these give 171 positive-positive conjunctions, 342
positive-negative conjunctions, and 2,907 antipodal triples: 3,420 forms.
This catalog is explicitly conditional on that external restriction theorem;
the Booleanization and infinity-degree reduction above are unconditional.

OEIS searches for the intermediate tuples/ranks found unrelated numerical
matches only. No sequence or novelty claim is made.

## Reproduction

- `src/e1_gmin_m4_prop15697.py`
- `tests/test_prop15697.py`
- `evidence/e1_gmin_m4_prop15697.json`
- `scripts/p19_slack20_allb2_lift_cpsat.py`

Bounded edge-lift trials at the three surviving degrees returned `UNKNOWN`.
They are diagnostics only and are not used as evidence.

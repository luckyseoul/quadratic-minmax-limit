# Complete independent root proof reviews

2026-09-05. No mathematical computation or signing census was used.

## Full-strength boundary likelihood

Input: `NOTE_2026-09-05_FULL_STRENGTH_BOUNDARY_LIKELIHOOD.md`.
SHA-256: `8703433f6118f00dd589d711e9541f558489caa3d13059f8e71405333401fdb2`.
Complete independent root read: PASS.

Checked the discrete planted-channel identity, Price derivative factor
`1/(2pi)`, pair-dependent Gaussian boundary law, exact mixed posterior
covariance integral, integrable singular endpoint and sign coupling.
Independently reconstructed all four order-three width cases from row
and column sums, the `Delta W=-2` sign, the `log128` errors, actual
global half-product optimality by switching/global-negation equivalence,
and both nonzero covariance eigenvalues in the exact endpoint range.
The endpoint support assertion was expanded with its spectral proof
before this final read. A second complete independent review by
optimized_profile_docs_gate also passed on this final hash.

The finite counterexample refutes a coordinatewise sign premise only.
It does not refute the averaged derivative, a selected cross block,
an asymptotic comparison, or original convergence.

## Selected half restrictions

Input: `NOTE_2026-09-05_SELECTED_HALF_RESTRICTION.md`.
SHA-256: `c8a9aa0b8c44fb14f444955fbe3eec8cba8e7f19c01fb8eeb2596418d3416c02`.
Complete independent root read: PASS.

Checked the disjoint positive/negative phase inequality using independent
block flips; the explicitly listed odd disjointness cycle, including
its closing edge; the coloring contradiction even when both phases of
a subset exceed the threshold; and the even-order exchange argument.
The one-vertex one-sided maximum changes by at most `a(n-1)`, not
twice that amount, because both adjacent sets share the same restricted
form and both maxima lie in its common length-`a(n-1)` interval.
Checked the complementary-color argument, the path in the subset
exchange graph, the application to actual larger-order minimizers,
and all normalization/error factors.

Inspected solution.md Proposition 7.3: its fixed-partition inequality
is `k m_n<=2m_(kn)` and at `k=2` yields only ordinary monotonicity.
The new inequalities `m_(2n+1)>=2m_n` and
`m_(2n)>=2m_n-(n-1)` use selected subsets, with no assumption that
the restrictions themselves minimize. The normalized factor remains
`sqrt(2)>1`; these bounds do not prove convergence or the proposed
sharp `2^(-3/2)` proportional restriction.

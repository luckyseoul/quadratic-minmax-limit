# Root review: Boolean shell remainder and fixed-block regularization

2026-09-05. Mathematical verdict: PASS within the two stated scopes.
Original convergence and the sharp all-shell upper remain OPEN.

## Complete reads and reviewer roles

The root completely read the 264-line cross-only regularization proof
and both of its exact repository prerequisites:
`NOTE_2026-09-05_NORM_CAP_FIELD_RESPONSE.md` and
`NOTE_2026-09-05_SAME_ORDER_SPECTRAL_REGULARIZATION.md`.
The only requested correction was the explicit epsilon_B>0 declaration.
Reversing exactly that one-line change in a read-only stream recovers
the initial complete-read SHA-256
`fcb20c448f022caf374c43ae1fca610e62164862a6d63d75b25c305bf8c34a71`.
The final independently reviewed source is
`27d9ab77768e8b7afa2d48d041cf3fe6bf3b66e8b16e481ca12abcf906a28d4f`.

The root also completely read all 322 lines of the Boolean ellipsoid
proof at SHA-256
`fdbe8a48112a7490e64c43fe116a48b5e28ea9611209ee0548dcce0a6d16ff78`.
The root read the final two-line introductory clarification, which
describes the expected Boolean-coordinate completion-square penalty
without calling its deterministic center the random ellipsoid maximizer.
The complete source diff contains only that change. The final source is
`ede1b62a26a636179d918ba84a48d122ab013c38175bdb9cd164bcfd8bfeb9aa`;
every formula and proof is unchanged. Both updated imported receipts
were completely read, including this delta and their retained initial
complete-read provenance.
The root contributed the improved completion-parameter optimization
and scalar minimization during development, so this part of the receipt
is a collaborative-author check, NOT an independent-author review.
The separate docs-worker complete review provides independent review
for the Boolean proof. The proof worker independently reviewed the
cross-only regularization theorem; its docs-worker receipt is author-only.

The canonical imports preserve the final source bytes exactly, including
the regularization proof's final blank line. The four imported receipts
were completely read, including their computation-provenance limitations.

## Fixed-internal-block audit

The finite rectangular tensor-rounding inequality gives the single
diagonal majorizer of the bipartite dilation with trace Lambda beta(B).
Bipartite conjugation supplies both signs using the same diagonal.
The thresholded row and column sets have the literal stated size, and
principal restriction bounds the retained cross rectangle's operator.

The rectangular Gaussian-net-style Rademacher tail and Boolean-maximum
tail have joint positive success probability for every n>=2. The
operator factor eight and Boolean factor 2n sqrt(s) are correct; no
independence between the two success events is required.

The two independent whole-set spin flips remove the selected cross
strips AND the two internal A-cuts. Restoring the fixed internal
blocks incurs exactly the two rectangular beta terms, which cannot
be discarded. The operator cap and boundary Cauchy--Schwarz estimate
give the printed bounded-A constant. The source-selection corollary
uses the actual prior same-order theorem, retaining epsilon_A and
epsilon_B separately and the epsilon_A^(-4) epsilon_B^(-2) cross cap.
Neither regularized object is declared an exact optimizer.

## Boolean remainder audit

Completion of the square is pointwise before shell maximization.
The diagonal lower bound depends only on the Gaussian coordinates'
distances to signs. Its expectation is exact without coordinate
independence. Both factors in (2) are nonnegative by their quadratic
and PSD-trace representations. Optimizing the positive completion
parameter gives (3), with the appropriate infimum when a factor is
zero. The weaker constant-distance inequality gives (4), not an
unjustified universal multiplication of an ellipsoid width by sqrt(kappa).

The positive diagonal P=E limit gives the exact unrestricted cube
width for every covariance, including nonuniform marginal variances.
The diagonal-affine trace bound separately uses a valid contraction
resolvent inequality. Its eta=0 Cauchy upper is not asserted to equal
the cube width for every nonuniform diagonal D.

The two scalar branches, discriminant and endpoint infima in (14)-(16)
are correct on the full square [-1,1]^2. In the improving branch the
denominator of eta_* is positive and the first derivative zero lies
in (0,1], with eta=1 interpreted only as a limiting metric. The
negative-parameter branch is the stated sign reversal. The two-trace
formula retains the shared trace with its opposite signs.

The actual cross diagnostic keeps the independent Hermite cushion,
and the formal uncorrected covariance is never sampled when indefinite.
Its exact expressions improve the scalar bound but do not reach the
sharp target, and its parameter point is not promoted to an actual
conditional-optimizer counterexample.

## Computation and scope

The Boolean proof author evaluated its elementary decimal diagnostic
once with local Python; the independent reviewer checked it once with
local JavaScript arithmetic. Those two small computations were not
offloaded as the repository instructs. Their exact methods are recorded
in their respective receipts. No unchanged scalar replay was requested
merely to obtain a different provenance; future computational checks
must follow the offload rule. The exact formulas and analytic proof
reviews, not the decimals, support these theorems.

No signing enumeration, simulation, optimizer experiment, source-code
or predicate change is part of this milestone. Neither theorem gives
a sharp all-orders comparison or original convergence.

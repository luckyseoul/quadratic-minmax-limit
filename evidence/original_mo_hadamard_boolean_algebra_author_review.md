# Collaborative author receipt: invariant Boolean-algebra lower

2026-09-05. This is author provenance and self-review, not an
independent-review receipt.

Source: `/tmp/original_mo_hadamard_boolean_algebra_lower.md`

Source length: 363 lines.

Source SHA256:
`68ce3f2f2a8fa2280208a9e145f508b6c2b2520d81e83185f579aaac89838a5d`

The parent proposed using actual paired Boolean vectors with controlled
means to turn a background completion into a true Boolean lower bound.
The author/integrator, `optimized_profile_exact`, identified the exact
mean grid for the original tensor background, observed the obstruction
from its inter-block mixing, and supplied the modified base Hadamard
`H'_4=(J_4-2I_4)P_4` that fixes a full Boolean algebra.

The dense independent block grids, nested overlap construction, and
Lambda_I formula were checked collaboratively with the parent. The
parent supplied the clean PSD equality argument and the symmetric
dilation/duplication route to a positive quadratic top frame. The
author verified those arguments and wrote the complete integration
with the actual sparse-flip sign construction.

The `optimized_profile_proof` collaborator's separate scalar-template
Gamma proof supplies the clipped-Gaussian quantity D(q), its exact
Gaussian enclosures, and the stored single remote rational certificate.
This additive note reuses that result without a new constant search or
verification run. The author supplied the direct same-vector rounding
proof transferring `1+D(q)` to an ACTUAL Boolean lower in the new
positive-frame model. That transfer is not claimed for arbitrary
nonsymmetric templates or arbitrary complete sign matrices.

The author wrote and fully reread all 363 source lines. The parent then
reported a complete full-read PASS with no correction, including the
exact fiber counts, dense grids, PSD actual limit, positive-frame
hypothesis, and changed-model scope of the dilation. The separately
assigned independent reviewer records that review in its own receipt.

All three analytic proof sources relevant to this work remain frozen:
the original sparse-flip construction, this additive Boolean-algebra
note, and the scalar-template Gamma bound. This receipt does not alter
their content or silently incorporate later coordinate-optimization
ideas.

No mathematical computation, sign search, sample construction, or test
was run for this additive note. Local tools only read and wrote proof
text, inspected existing proof/certificate evidence, and computed byte
hashes and line counts. The original conditional optimizer, intrinsic
source compatibility, and original MO convergence remain unresolved.
